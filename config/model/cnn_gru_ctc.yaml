# @package _global_
module:
  _target_: emg2qwerty.lightning.CNNGRUCTCModule
  in_features: 528  
  mlp_features: [384]
  block_channels: [24, 24, 24, 24]
  kernel_width: 32  
  gru_hidden_size: 256   
  gru_layers: 2          
  gru_dropout: 0.2       

datamodule:
  _target_: emg2qwerty.lightning.WindowedEMGDataModule
  window_length: 8000  # 4 sec windows for 2kHz EMG (change for freq) ? 
  padding: [1800, 200]  # 900ms past context, 100ms future context (change for freq) ? 
